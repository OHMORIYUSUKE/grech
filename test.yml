name: OS-lecture-check

config:
  mysql_user: root
  mysql_password: root

check:
  capter_01:
    - name: 01
      cmd: ls -l
      regexp: [.......r.x]
      working-directory: /var/www
      message: |
        エラー
        ここに注目１
    - name: 02
      cmd: ls -l
      regexp: [.......r.x, \.html]
      working-directory: /var/www
      message: |
        エラー
        ここに注目２
  capter_02:
    - name: 01
      cmd: ls -l
      regexp: [.......r.x]
      working-directory: /var/www
      message: |
        エラー
        ここに注目１
    - name: 02
      cmd: ls -l
      regexp: [.......r.x, \.html]
      working-directory: /var/www
      message: |
        エラー
        ここに注目２
